<h1>All tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(id: task.id) %></li>
    <p><%= task.details %></p>
  <% end %>
</ul>
<p><%= link_to " + Add a new task", tasks_new_path %></p>